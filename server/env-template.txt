# Copy this file to .env and update with your MongoDB Atlas credentials

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Atlas Configuration
# Replace YOUR_PASSWORD and YOUR_CLUSTER_URL with your actual values
MONGODB_URI=mongodb+srv://meesho-user:YOUR_PASSWORD@YOUR_CLUSTER_URL.mongodb.net/meesho-clone?retryWrites=true&w=majority

# JWT Secret (for future authentication features)
# Generate a strong secret for production: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here

# CORS Configuration
# For production, set this to your client URL
CORS_ORIGIN=http://localhost:3000

# Production Environment Variables (for Render)
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/meesho-clone?retryWrites=true&w=majority
# CORS_ORIGIN=https://your-client-app.onrender.com
