# Copy this file to .env and update with your MongoDB Atlas credentials

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Atlas Configuration
# Replace YOUR_PASSWORD and YOUR_CLUSTER_URL with your actual values
MONGODB_URI=mongodb+srv://meesho-user:YOUR_PASSWORD@YOUR_CLUSTER_URL.mongodb.net/meesho-clone?retryWrites=true&w=majority

# JWT Secret (for future authentication features)
JWT_SECRET=your-super-secret-jwt-key-here

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
